
// Re-export everything from services for backward compatibility
export * from '../services/reservationApi';

// This file now serves as a compatibility layer
// All actual API logic is in the services folder
